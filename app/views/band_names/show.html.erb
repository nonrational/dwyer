<% content_for :breadcrumbs do %>
    ⇒ <%= @band_name.name %>
<% end %>

<%= render @band_name %>

<% if current_user && (current_user.admin? || current_user.owns?(@band_name)) %>
  <%= link_to 'Edit', [:edit, @band_name] %>
<% end %>

